services:
  web:
    build: .
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www/html/
    networks:
      - db_network

networks:
  db_network:
    name: mariadbnosync_default 
    external: true